{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:solutionspool:events:user.updated:v1",
  "title": "UserUpdatedV1",
  "description": "Published by User Service after a user's profile is updated. Consumed by Auth Service to keep the identity's display name in sync.",
  "type": "object",
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique per event. Consumers can use it for idempotency / de-duplication."
    },
    "event": {
      "const": "user.updated"
    },
    "userId": {
      "type": "string",
      "minLength": 1,
      "description": "Identity id, owned by Auth Service (same id as UserProfile.userId)."
    },
    "displayName": {
      "type": "string",
      "minLength": 1
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": ["eventId", "event", "userId", "displayName", "updatedAt"],
  "additionalProperties": false
}
